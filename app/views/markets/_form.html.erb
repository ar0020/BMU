<%= form_for(@market) do |f| %>
  <% if @market.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@market.errors.count, "error") %> prohibited this market from being saved:</h2>

      <ul>
      <% @market.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :balance_string, "Current Balance" %><br>
      <%= f.text_field :balance_string %>
    </div>
    <div class="field">
      <%= f.label :monthly_account_rate %><br>
      <%= f.text_field :monthly_account_rate %>
    </div>
    <div class="field">
      <%= f.label :is_active %><br>
      <%= f.check_box :is_active %>
    </div>
    <%= f.hidden_field :user_id%>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
