<%= form_for(@mortgage) do |f| %>
  <% if @mortgage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mortgage.errors.count, "error") %> prohibited this mortgage from being saved:</h2>

      <ul>
      <% @mortgage.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :current_balance %><br>
      <%= f.text_field :current_balance %>
    </div>
    <div class="field">
      <%= f.label :monthly_account_rate %><br>
      <%= f.text_field :monthly_account_rate %>
    </div>
    <div class="field">
      <%= f.label :is_active %><br>
      <%= f.check_box :is_active %>
    </div>
    <%= f.hidden_field :user_id%>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
