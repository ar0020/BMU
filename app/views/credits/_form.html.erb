<%= form_for(@credit) do |f| %>
  <% if @credit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@credit.errors.count, "error") %> prohibited this credit from being saved:</h2>

      <ul>
      <% @credit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :balance_string, "Initial Balance" %><br>
      <%= f.text_field :balance_string %>
    </div>
    <div class="field">
      <%= f.label :monthly_account_rate %><br>
      <%= f.text_field :monthly_account_rate %>%
    </div>
    <%= f.hidden_field :user_id%>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
